# include<stdio.h>

void max(int *num, char **ch)
{
	char cht[20];
	int t;
	int i;
	
	for() 
} 

int main(void)
{
	int n;
	int i, num[1000];
	char ch[1000][20];
	
	while(scanf("%d", &n) != 0)
	{
		for(i=0; i<n; ++i)
		{
			scanf("%d ", &num);
			gets(ch[i]);
			//printf("%s", ch[i]);
		}
		
		max(num, ch);
	}
} 
